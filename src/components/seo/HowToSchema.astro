---
import JsonLd from './JsonLd.astro';
import { howToSchema, type HowToStep } from '@lib/schema';

interface Props {
  name: string;
  steps: HowToStep[];
}

const { name, steps } = Astro.props;
---

<JsonLd data={howToSchema(name, steps)} />
