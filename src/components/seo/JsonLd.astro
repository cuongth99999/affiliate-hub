---
interface Props {
  data: Record<string, unknown> | Record<string, unknown>[];
}

const { data } = Astro.props;
const payload = Array.isArray(data) ? data : [data];
---

{payload.map((d) => <script is:inline type="application/ld+json" set:html={JSON.stringify(d)} />)}
