---
import JsonLd from './JsonLd.astro';
import { organizationSchema, websiteSchema } from '@lib/schema';

const siteUrl = Astro.site?.toString().replace(/\/$/, '') ?? '';
---

<JsonLd data={[organizationSchema(siteUrl), websiteSchema(siteUrl)]} />
